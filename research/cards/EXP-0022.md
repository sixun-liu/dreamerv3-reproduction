# EXP-0022：DreamerV3 Minecraft runtime-derived milestone integrity repair

状态：`preregistered`，高成本运行：`not approved`，人工看图：`pending`。

## 一句话问题

由每个真实Minecraft环境的_inv_keys导出里程碑标量并增加reset-zero与索引有效性gate后，三环境并行evaluator能否恢复可信的逐worker里程碑口径？

## 阶段与基座

- 阶段：`exploration`
- 当前主要矛盾：EXP-0021证明三worker调度可行，但静态inventory顺序导致伪cobblestone和wooden_pickaxe，必须先恢复评测证据完整性才能测试加速。
- Canonical baseline：`DV3-2026-MINECRAFT-DIAMOND-50M-100K`
- 模板：`instrumentation`；证据权限：`diagnostic_only`
- 假说族：`minecraft-terminal-evaluation-integrity`；工作模式：`instrumentation`
- 唯一变量：仅将里程碑提取从父进程静态mc.ALL_ITEMS解释改为各环境进程基于真实_inv_keys导出的log/eval_milestone标量，并增加完整性校验；其余协议固定。
- 预期动作：三个环境各构造runtime milestone wrapper，在首个reset transition报告真实索引和全零里程碑；SingleEpisodeHold位于其外侧，worker0仍为固定视频源。
- 完成正向信号：128步smoke中三个worker的tracked milestone索引全部存在、互异、在inventory范围内且跨worker一致。；每个worker的reset transition全部tracked milestone值为0，之后汇总只消费log/eval_milestone字段而不再读取静态mc.ALL_ITEMS。；三个worker各恰有一个is_first/is_last、无额外reset、动态worker0视频、3个Java、无OOM且完整清场。

## 假说

若EXP-0021唯一的里程碑失真来自外部mc.ALL_ITEMS静态排序，则使用每个环境实例的运行时_inv_keys后，三个reset帧的全部里程碑都应为0，每个worker的索引应存在、互异、在范围内且跨worker一致，同时保持单episode调度与动态视频证据。


## 替代解释

- 若reset仍非零，可能是运行时inventory_max初始化或wrapper层级语义本身有问题，而不只是排序错误。

## 控制项

- 固定runtime6723fc1、EXP-0020 step200000 checkpoint hashes、minecraft_diamond、size50m、agent seed10000、3 worker、每worker一局、128步smoke、video stride4。

## 主指标

- Primary: runtime_mapping_valid、reset_milestones_zero、cross_worker_mapping_consistent三门全部为true。
- Tail: episode boundaries、hold accounting、video dynamics、peak Java/RAM/GPU、OOM、disk和cleanup。

## 证据四联

- 数值/概率：
- 空间/视觉：
- 时序/上尾：
- 因果干预：

## 停止条件

- 任一reset里程碑非零、索引缺失/重复/越界/跨worker不一致、额外episode、静态视频、OOM或清场失败则不运行完整三局。

## 人工审查

`/root/autodl-tmp/Artifacts/dreamerv3/review/EXP-0022-minecraft-runtime-milestones`

## 最终裁决

- Verdict：`runtime_milestones_repaired_parallel_eval_selected`
- Decision：`promising_unresolved`
- 解释：三环境并行evaluator在当前size50m、RTX5090与16核配置上可作为后续Minecraft checkpoint默认终点评测路径；这是工程执行路径选择，不是科学主张晋级，串行脚本保留用于回退和抽验。
- 预期动作：`occurred`；完成信号：`confirmed`
- 进展：`efficiency`，修复评测证据口径后，将固定三回合端到端墙钟从32.87分钟降至13.27分钟，并以动作归一口径确认1.42x吞吐提升。
- 耗时：wall `0.271h`；compute `0.271h`
- 观察：运行时_inv_keys映射修复通过smoke和完整三回合全部完整性门；reset里程碑全零，索引有效、互异且跨worker一致。；完整评测796秒，相对串行1972秒端到端2.477x；动作归一吞吐1.417x，超过预注册1.5x墙钟选择门。
- 限制：Minecraft world seed不可控，本轮总动作数少于串行基线；2.477x包含随机episode长度差异，工程调度收益更保守地由1.417x动作归一吞吐描述。；该cycle只选择评测执行路径；三局分数与里程碑不构成策略质量配对比较，也不提升论文Diamond复现主张。
- 下一步：以EXP-0020精确200K为只读源，使用patched runtime6723fc1、envs4、size50m和ratio32预注册到精确500K的有界增量；终点采用已选择三环境评测。随后再做size50m/size100m资源与5K吞吐gate。
- 人工确认：`pending`
- Scoreboard：`not_required`；Claim registry：`not_required`
