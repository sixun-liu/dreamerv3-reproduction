# EXP-0023：DreamerV3 Minecraft exact 200K to 500K increment and parallel evaluation

状态：`preregistered`，高成本运行：`approved by user`，人工看图：`pending`。

## 一句话问题

从EXP-0020精确200K checkpoint与replay继续到精确500K后，当前size50m方案能否保持工程完整性，并在新增300K replay或固定三环境终点评测中从wooden-pickaxe推进到cobblestone？

## 阶段与基座

- 阶段：`exploration`
- 当前主要矛盾：EXP-0020在200K首次观察到wooden_pickaxe但无cobblestone，EXP-0022已选择可信且更快的并行evaluator；当前需有界检验额外300K是否继续推动技能链。
- Canonical baseline：`DV3-2026-MINECRAFT-DIAMOND-50M-100K`
- 模板：`formal`；证据权限：`formal_candidate`
- 假说族：`minecraft-incremental-learning`；工作模式：`mixed`
- 唯一变量：训练绝对预算从精确200K增加到精确500K environment steps；源、runtime6723fc1、seed0、size50m、ratio32、envs4、reward、wrapper与评测协议固定。
- 预期动作：以EXP-0020为只读源reflink克隆checkpoint/replay，以envs4新增恰好300000个唯一transition并保存step500000 checkpoint；随后固定agent seed10000用三环境各一局评测并固定worker0视频。
- 完成正向信号：EXP-0020源checkpoint/replay树哈希前后不变；目标保留200000个源stepid并恰好新增300000个无重复stepid，最终checkpoint精确step500000。；训练loss有限、出现ratio32、4个Java worker、无OOM/系统盘泄漏且完成临时目录与GPU/Java/Xvfb清场。；终点评测运行时milestone索引有效且跨worker一致、reset全零、每worker恰一局、动态worker0视频和完整资源清场。

## 假说

若当前DreamerV3 Minecraft策略随额外300K交互继续推进技能链，则目标应精确新增300000个唯一transition、保持有限loss与ratio32，并在新增replay或三环境评测至少一次观察到cobblestone；wooden_pickaxe应在新增replay中再次出现。


## 替代解释

- 若新增replay仍无cobblestone，可能是500K预算仍不足、size50m容量/探索受限，或Minecraft稀疏技能链的高方差，而不是训练管线失败。
- 若新增replay有cobblestone但三局评测没有，可能是有限三局与不可控world seed漏检，而不是能力不存在。

## 控制项

- 固定runtime6723fc1、EXP-0020源树哈希、minecraft_diamond、size50m、seed0、ratio32、envs4、精确500K终点；评测固定agent seed10000、3 worker、每worker一局、36K上限和worker0视频。

## 主指标

- Primary engineering: exact300K new unique stepids、source unchanged、step500K、finite loss、ratio32、resource and cleanup gates。
- Primary learning: cobblestone在新增300K replay或固定三局评测至少出现一次；wooden_pickaxe在新增replay再次出现。
- Tail: milestone trajectory counts/first scan position、三局return/length、训练墙钟/FPS、CPU/RAM/GPU、并行评测墙钟与hold counts。

## 证据四联

- 数值/概率：精确源/目标/new stepid、checkpoint step、有限 loss、ratio32、三局 return 与里程碑命中数、CPU/RAM/GPU 和墙钟。
- 空间/视觉：预先固定 worker0 第0局的动态视频与首帧，不按分数或里程碑挑选。
- 时序/上尾：新增 replay 的各里程碑首次扫描位置与成功轨迹数、训练 score/loss 序列、三局 episode 长度和并行 hold 账。
- 因果干预：只改变绝对训练预算 200K→500K；模型规模、seed、环境数、ratio、runtime、reward、wrapper 与终点评测协议均固定。

## 停止条件

- GPU忙、repo/runtime/source哈希漂移、目标已存在、系统盘<5GiB或数据盘<10GiB时不启动。
- 训练nonfinite、OOM、源被修改、非精确500K、replay数量/stepid不符或清场失败则不评测；不依据中途里程碑提前停止或追加到1M。

## 人工审查

`/root/autodl-tmp/Artifacts/dreamerv3/review/EXP-0023-minecraft-exact-500k`
