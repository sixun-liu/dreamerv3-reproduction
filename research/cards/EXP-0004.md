# EXP-0004：DreamerV3 walker_walk three-seed 500K replication

状态：`preregistered`，高成本运行：`not approved`，人工看图：`pending`。

## 一句话问题

当前作者post-Nature实现按冻结DMC proprio协议运行seeds0,1,2时，最后30K environment-step training-return聚合能否复现论文Table4 walker_walk的936性能包络，并说明早期曲线差异是否跨seed稳定？

## 阶段与基座

- 阶段：`reproduction`
- 当前主要矛盾：用三个clean seeds区分EXP-0001前期落后/后期追入是单seed现象还是当前代码代际的稳定训练动力学。
- Canonical baseline：`DV3-NATURE-DMCP-WALKER-12M`
- 模板：`replication`；证据权限：`replication_candidate`
- 假说族：`dmcp-walker-author-reimplementation`；工作模式：`practice`
- 唯一变量：训练seed为0、1、2；代码、配置、任务、预算、评估seed和分析口径固定。
- 预期动作：顺序运行三个250000 agent-decision训练run（repeat2=500K environment steps），每个自然结束保存step250000 checkpoint，再以固定eval seed10000运行40000 decisions。
- 完成正向信号：三个seed均有train/eval/root completed信标、精确step250000 checkpoint、完整metrics/scores/config/stdout；每个eval至少64个有限episodes且无train updates。

## 假说

若当前作者实现保留论文级walker_walk性能，则三seed全部自然完成后，每seed最后30K episode mean的跨seed均值应落入官方935.752±2×25.905区间[883.942,987.562]，且至少2/3 seed落入官方逐seed最终范围[890.956,968.491]。

## 复现目标

- 类型：`author_reimplementation`
- 目标主张：DreamerV3 Nature Table4 DMC proprio walker_walk score 936 at500K environment steps.
- 参考产物：/root/autodl-tmp/dreamerv3/scores/dmc_proprio-dreamerv3.json.gz SHA256=8182860a8a56dc56836c319fde9b941376621e1e0d474141c7d174ab833cc7f4
- 协议匹配：task=dmc_walker_walk, configs=dmc_proprio+size12m, dm-control1.0.43, MuJoCo3.10.0, proprio, repeat2,16 envs。；每seed 250K agent decisions=500K environment steps；train_ratio512；training episode/score为主指标。；论文值恢复为官方五seed最后三个10K点mean=935.752；本地按每seed最后30K完整episodes mean后跨seed聚合。；当前runtime5168475是2026作者重实现加默认关闭的仪器补丁，不是论文原始代码；固定checkpoint eval另列。


## 替代解释

- 若三seed都早期落后但最终达标，差异更可能来自2023到2026代码/环境代际而非seed0偶然性。
- 同步16 env使本地episode落点与官方10K曲线点不完全对齐，最终窗口统计可能存在小幅聚合差异。
- 三个seed少于官方五seed，可能进入包络但方差估计仍不足以声称exact replication。

## 控制项

- runtime5168475、配置hash、官方score hash、训练预算、final checkpoint、eval seed10000与无失败seed替换固定。

## 主指标

- 主指标：三seed各自(470K,500K]完整training episodes mean及跨seedmean/std；门为跨seedmean[883.942,987.562]且至少2/3单seedmean[890.956,968.491]。
- 时序指标：10K bins的mean/median、250K差值、首次进入官方逐seed包络的step、曲线末段和最差seed。
- 独立checkpoint eval：每seed64+有限episodes的mean/median/std/min/max；只验证consumer并作为次指标。

## 证据四联

- 数值/概率：
- 空间/视觉：
- 时序/上尾：
- 因果干预：

## 停止条件

- 任一seed出现NaN/Inf、异常退出、checkpoint step错误、GPU冲突、数据盘可用<10GiB或单训练run墙钟>2小时即停止后续启动并保留失败seed。
- 不按中途分数早停、不替换失败seed、不调参；得分前纯工程失败须登记为invalid attempt后才能原配置重试。

## 人工审查

`/root/autodl-tmp/artifacts/dreamerv3/review/EXP-0004-walker-three-seed`
