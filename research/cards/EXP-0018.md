# EXP-0018：DreamerV3 synchronous multi-environment exact stop instrumentation

状态：`preregistered`，低成本 instrumentation：用户长期 goal 已授权，人工看图：`not_required`。

## 一句话问题

将train loop最后一轮driver请求限制为剩余step后，envs=4从EXP-0012 step100000恢复并新增40步时，能否精确结束于100040而非旧语义的100048？

## 阶段与基座

- 阶段：`exploration`
- 当前主要矛盾：EXP-0017因同步Driver停止粒度结束于200008；当前先以最低成本修复并验证精确停止，再恢复正式200K路线。
- Canonical baseline：`DV3-2026-MINECRAFT-DIAMOND-50M-100K`
- 模板：`instrumentation`；证据权限：`diagnostic_only`
- 假说族：`minecraft-exact-stop-integrity`；工作模式：`instrumentation`
- 唯一变量：runtime train.py的driver调用由固定steps=10改为min(10, remaining_steps)，其余runtime、source、envs4、seed0、ratio32与恢复方法固定。
- 预期动作：单测覆盖旧实现会越界的非12整除预算；真实EXP-0012恢复副本新增40个唯一transition并保存step100040 checkpoint。
- 完成正向信号：runtime train单测精确断言env_steps等于请求预算，并覆盖恢复后的第二段精确终点。；真实Minecraft smoke源哈希不变、旧100000 stepid全保留、恰好新增40个唯一stepid、checkpoint step100040。；无OOM、系统盘泄漏或GPU/Java/Xvfb/临时目录残留；不读取score形成裁决。

## 假说

若+8步来自固定driver steps=10与四环境12-step调用量子，则remaining-step修复应在dummy unit test与真实Minecraft恢复smoke中都把checkpoint和replay精确停在目标，且不改变常规完整chunk训练语义。


## 替代解释

- 若修复后仍越界，可能是logger counter/replay reset transition而非外层driver请求；需冻结当前失败并追踪callback计数。

## 控制项

- 固定EXP-0012源树哈希、runtime base5168475、envs4、debug=false、size50m、ratio32和40步新增预算。

## 主指标

- Primary: checkpoint step=100040、target unique stepids=100040、new unique stepids=40；Tail: source unchanged、worker starts、OOM/内存/磁盘/清场。

## 证据四联

- 数值/概率：精确核验 checkpoint step、source/target/new unique stepid 与单测边界矩阵。
- 空间/视觉：本 cycle 不评价策略或画面，视觉证据不适用。
- 时序/上尾：记录启动墙钟、内存/显存峰值、OOM、磁盘和清场；不以 score 裁决。
- 因果干预：只改变 train loop 最后一轮 driver 请求；旧 runtime 的 `200008` 作为已冻结反例，真实 smoke 固定其余恢复语义。

## 停止条件

- 单测失败、源漂移、GPU忙、目标存在或真实smoke任何完整性门失败即停止；不得在同一输出重跑。

## 人工审查

`/root/autodl-tmp/Artifacts/dreamerv3/review/EXP-0018-exact-step-stop`

## 最终裁决

- Verdict：`exact_stop_instrumentation_passed`
- Decision：`promising_unresolved`
- 解释：EXP-0017的+8越界确由固定10-step Driver请求与4环境12-transition推进量子造成；remaining-step补丁在可达目标上恢复精确停止语义。
- 预期动作：`occurred`；完成信号：`confirmed`
- 进展：`uncertainty_reduction`，以约3分钟真实smoke闭合多环境精确停止根因和修复，避免再次浪费正式100K训练。
- 耗时：wall `0.053h`；compute `0.051h`
- 观察：dummy exact-step边界测试8/8通过，真实Minecraft从100000精确到100040且新增40唯一transition。；源隔离、agent更新、4 worker起点、有限loss、资源和清场门均通过；短smoke不以ratio32日志样本作裁决。
- 限制：本cycle只验证instrumentation，不评价策略质量或长程吞吐；正式训练仍必须观测ratio32并通过完整门。；剩余预算不能被环境数整除时补丁会显式拒绝，而不是静默越界；正式目标需满足可达性。
- 下一步：另开有界效率cycle，在同一恢复语义下只比较envs4附近候选；冻结局部最优后从原始EXP-0012新输出重跑正式200K。
- 人工确认：`not_required`
- Scoreboard：`not_required`；Claim registry：`not_required`
