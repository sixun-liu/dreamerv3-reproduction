# EXP-0001：DreamerV3 DMC walker_walk 500K 单 seed 复现

状态：`complete`，冻结运行：`completed`，人工看图：`pending`。

## 一句话问题

当前作者公开重实现按论文 DMC proprio 协议运行时，seed 0 的 walker_walk 学习曲线是否进入官方五 seed 包络？

## 阶段与基座

- 阶段：`reproduction`
- 当前主要矛盾：恢复并执行 Nature-era DMC proprio walker_walk 代表性结果。
- Canonical baseline：`DV3-NATURE-DMCP-WALKER-12M`
- 模板：`replication`；证据权限：`replication_candidate`
- 假说族：`paper-result-replication`；工作模式：`mixed`
- 唯一变量：无方法变量；复现冻结协议本身
- 预期动作：episode/score 随 environment steps 上升
- 完成正向信号：到达 250000 agent decisions（500000 environment steps）且生成 metrics/scores/checkpoint

## 假说

在 500K environment steps 内，episode score 将持续上升，并在中后期进入官方五 seed 曲线包络。

## 复现目标

- 类型：`author_reimplementation`
- 目标主张：DreamerV3 在 DMC proprio walker_walk 的 500K environment-step 分数约为 936
- 参考产物：scores/dmc_proprio-dreamerv3.json.gz sha256=8182860a8a56dc56836c319fde9b941376621e1e0d474141c7d174ab833cc7f4
- 协议匹配：size12m; repeat2; 16 envs; replay ratio512; 500K env steps; seed0；代码为 Nature-era 作者公开重实现而非论文原始 commit


## 替代解释

- 若曲线偏低，可能来自代码代际、dm-control/MuJoCo 版本、训练回报与论文评估口径或单 seed 方差，而非算法失效。

## 控制项

- 不修改算法；固定 commit e3f0224、expanded config、seed0 和官方 JSON。

## 主指标

- 主指标：10K environment-step 分箱 episode score 相对官方 seed envelope；尾指标：loss/grad、失败 episode、吞吐和 checkpoint。

## 证据四联

- 数值/概率：
- 空间/视觉：
- 时序/上尾：
- 因果干预：

## 停止条件

- 10K env-step pilot 推算无法在剩余约8小时预算内完成；NaN/Inf；OOM；无 checkpoint；或重复 GPU 进程。

## 人工审查

`/root/autodl-tmp/artifacts/dreamerv3/review/EXP-0001-dmcp-walker-walk-500k-s0`

## Checkpoint

- 进展：`efficiency`；Pilot gate passed at logger step 18144 environment steps: policy FPS 70.47 agent decisions/s, GPU ~25.6GB, no NaN/Inf, replay ratio 454 approaching 512; projected remaining training about 0.95h, so continue to 500K.
- 本段 compute：`0.080h`
- 动作：`occurred`；完成信号：`pending`

## Checkpoint

- 进展：`uncertainty_reduction`；Midpoint at logger step 248672: 250K local bin median=476.12 (n=16), below official five-seed range 826.85-945.81; local curve is still rising, losses finite, replay ratio=520, policy FPS=63.93. Continue to completion; defer causal interpretation.
- 本段 compute：`0.530h`
- 动作：`occurred`；完成信号：`pending`

## 最终裁决

- Verdict：`late_catchup_into_official_seed_envelope`
- Decision：`promising_unresolved`
- 解释：当前作者公开重实现复现了walker_walk的后期性能量级和上升趋势，但早期样本效率未对齐官方曲线。
- 预期动作：`occurred`；完成信号：`confirmed`
- 进展：`metric`，完成代表性500K DMC结果，后期进入官方seed包络，同时保留早期明显落后的反证。
- 耗时：wall `1.120h`；compute `1.100h`
- 观察：490K同坐标bin中位914.2，进入官方735.6--955.0范围；最终窗口中位861.9。；250K中位476.1显著低于官方范围，约400K后追入包络；run自然完成且无NaN/崩溃。
- 限制：单seed；作者公开重实现与论文原始代码代际不同；dm-control/MuJoCo版本和训练回报/官方评估口径未完全闭环。
- 下一步：取证官方dmc_proprio JSON的代码版本与评估生成协议，区分早期差异来自lineage、environment还是metric semantics。
- 人工确认：`pending`
- Scoreboard：`updated`；Claim registry：`not_required`
