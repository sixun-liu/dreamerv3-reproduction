# EXP-0016：DreamerV3 Minecraft recovery environment scaling ladder

状态：`preregistered`，高成本运行：`not approved`，人工看图：`pending`。

## 一句话问题

在16核CPU、90GiB内存与RTX5090边界内，恢复场景的envs=4或8是否能在完整性不变时继续降低新增100K训练墙钟，还是envs=2已是最高性价比点？

## 阶段与基座

- 阶段：`exploration`
- 当前主要矛盾：EXP-0015已证明envs=2隔离恢复完整且稳态policy FPS为40.684；当前需用有界envs=4/8阶梯探针确定16核CPU配额下的最高性价比恢复吞吐，再冻结正式新增100K训练。
- Canonical baseline：`DV3-2026-MINECRAFT-DIAMOND-50M-100K`
- 模板：`instrumentation`；证据权限：`diagnostic_only`
- 假说族：`minecraft-recovery-throughput-scaling`；工作模式：`instrumentation`
- 唯一变量：从同一EXP-0012只读100K源分别创建隔离CoW恢复副本，只改变run.envs为4或8；envs=8由envs=4冻结门自动授权。
- 预期动作：每档恢复全部100K历史transition，建立与envs数相同的新worker起点并精确新增5040步；固定分析器按相同步数窗口计算稳态FPS、长程ETA与资源账。
- 完成正向信号：envs=4自然结束于105040并通过源哈希、105040唯一stepid、4个新worker起点、有限metrics、ratio32和资源清场门。；固定窗口至少各有5个有效样本，并依据10% FPS与8% ETA双门自动决定是否启动envs=8。；若envs=8运行，则以同一完整性门和5% FPS/ETA双门在envs=4与8之间选择；否则明确记录跳过原因。

## 假说

envs=4若能相对冻结的envs=2稳态policy FPS提升至少10%且预测新增100K ETA降低至少8%，则晋级并测试envs=8；envs=8仅在相对envs=4再提升5% FPS且ETA降低5%时晋级，否则保留前一档。


## 替代解释

- envs=4/8增加Java/Malmo并发却受16核quota、CPU throttling或驱动同步限制，CPU更忙但端到端训练不更快。
- 5040步的启动和checkpoint开销可能使端到端比值失真，因此需同时看冻结稳态窗口与预测100K ETA。

## 控制项

- 固定runtime5168475、EXP-0012源树指纹、Minecraft Diamond、size50m、seed0、ratio32、reward、wrapper、绝对终点105040与CoW恢复方法。
- EXP-0015 envs=2 metrics/integrity/resource五个哈希只读固定；禁止score、inventory和GPU占用率参与选择。

## 主指标

- Primary: 20%至90%新增预算窗口、ratio28至36的policy FPS均值；envs4/2门1.10，envs8/4门1.05。
- Tail: 启动开销加100000/FPS的预测100K ETA；门分别要求降低8%和5%，另核验CPU/RAM/GPU/磁盘/OOM/清场。

## 证据四联

- 数值/概率：
- 空间/视觉：
- 时序/上尾：
- 因果干预：

## 停止条件

- 源或envs2证据漂移、GPU已有进程、根盘<5GiB、数据盘<10GiB、CoW/inode隔离失败时不启动。
- 任一档OOM、非有限值、stepid重复/丢失、源修改、内存峰值>72GiB、系统盘泄漏或进程残留时停止；envs4不晋级则禁止envs8。

## 人工审查

`/root/autodl-tmp/Artifacts/dreamerv3/review/EXP-0016-minecraft-recovery-scaling`

## 最终裁决

- Verdict：`envs4_selected_recovery_scaling`
- Decision：`promising_unresolved`
- 解释：在当前16核、90GiB、RTX5090和固定script=train/ratio32/size50m语义下，envs=4是已测试2/4/8档中的最高性价比训练配置；本cycle只选择执行配置，不评价策略质量。
- 预期动作：`occurred`；完成信号：`confirmed`
- 进展：`efficiency`，以约11分钟有界阶梯确定envs=4吞吐拐点，预测正式新增100K训练约34分钟。
- 耗时：wall `0.183h`；compute `0.183h`
- 观察：envs=4以32.33%稳态FPS提升和22.02%预测ETA缩短通过envs=2晋级双门，完整性与资源安全门全绿。；envs=8稳态FPS相对envs=4下降21.89%，预测ETA增加28.49%，因此保留envs=4并关闭继续扩档路线。
- 限制：单次5040步恢复诊断，结论只覆盖当前runtime、Minecraft Diamond、固定算法语义和envs集合2/4/8；不宣称JAX/XLA、NUMA或异步架构层面的理论全局最优。
- 下一步：从原始EXP-0012 100K源独立CoW克隆，以envs=4正式训练到绝对200K，再做固定agent seed10000三回合单环境评测和里程碑材料。
- 人工确认：`not_required`
- Scoreboard：`not_required`；Claim registry：`not_required`
