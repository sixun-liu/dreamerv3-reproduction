# EXP-0010：DreamerV3 DMC Vision Walker staged single-seed replication

状态：`preregistered`，高成本运行：`not approved`，人工看图：`pending`。

## 一句话问题

2024作者重实现按DMC Vision size12m协议训练Walker时，100K能否形成预注册学习趋势，并在通过gate后于1M进入公开10-seed末段数值包络？

## 阶段与基座

- 阶段：`exploration`
- 当前主要矛盾：既有正式证据仅覆盖DMC Proprio；需要建立像素输入的首个可审计学习与行为证据。
- Canonical baseline：`DV3-2024-DMCV-WALKER-12M`
- 模板：`replication`；证据权限：`replication_candidate`
- 假说族：`dmc-vision-walker-author-reimplementation`；工作模式：`practice`
- 唯一变量：No within-cycle algorithm intervention; consume and reconstruct 64x64 RGB image observations under frozen dmc_vision plus size12m configuration.
- 预期动作：Complete smoke, then exactly 50000 decisions; only if the preregistered score, integrity, ETA, and disk gates pass, resume the same replay/checkpoint to exactly 500000 decisions.
- 完成正向信号：Smoke checkpoint, replay, metrics, scores, integrity, and one-episode pixel evaluation all complete.；Pilot checkpoint is exactly step 50000 and an immutable 100K checkpoint copy plus machine-readable continuation gate exist.；If continued, terminal checkpoint is exactly step 500000 and full analysis plus independent 10-episode evaluation and preregistered episode video exist.

## 假说

100K末20K平均分至少200且较首20K提升至少100；通过后同一checkpoint/replay续至1M，末100K平均分进入官方10-seed末段范围。

## 复现目标

- 类型：`author_reimplementation`
- 目标主张：DreamerV3在DMC Vision Walker上于约1M environment steps达到公开学习曲线所示的高回报行为。
- 参考产物：/root/autodl-tmp/Code/DreamerV3/dreamerv3-runtime-2411-crossdomain/scores/dmc_vision-dreamerv3.json.gz SHA256=68dd7e83ee6af076734e1edb843a3816cd241736c92e91837e7cf460231b7b08
- 协议匹配：task=dmc_walker_walk; image=64x64 RGB; model=size12m; repeat=2; train_ratio=512; num_envs=16; seed=0; 100K gate then at most 1M environment steps


## 替代解释

- Single-seed improvement may reflect uncontrolled DMC environment RNG rather than stable reproduction.
- The 2024 author reimplementation and public curve export can differ from the 2023 paper artifact and aggregation.

## 控制项

- Freeze runtime commit, three expanded configs, reference hash, seed, step units, same-logdir continuation, and no-tuning rule before outcome inspection.

## 主指标

- Primary gate: integrity plus final-20K mean >=200 and improvement over first-20K >=100; final envelope: local final-100K mean inside official 10-seed tail range.
- Tail: fixed 10K curve bins, complete episode distribution, NaN/Inf/error markers, wall time, GPU memory/utilization, throughput, and disk growth.

## 证据四联

- 数值/概率：
- 空间/视觉：
- 时序/上尾：
- 因果干预：

## 停止条件

- Stop for OOM, unexpected NaN/Inf, traceback/nonzero exit, duplicate GPU process, config/checkpoint mismatch, ETA above 12h, or projected disk below 5GiB.
- Do not tune, replace seed, restart after disconnect, or continue past 100K unless the frozen gate passes.

## 人工审查

`/root/autodl-tmp/Artifacts/dreamerv3/review/EXP-0010-dmc-vision-walker-staged`

## Checkpoint

- 进展：`efficiency`；DMC Vision smoke completed naturally in 599s. Frozen config equals runtime config; checkpoint step8192; 16 replay files, 16 finite score rows and 19 finite metric rows; no unexpected nonfinite values or error markers. A separately loaded pixel-policy episode completed in501 decisions with return189.99, nonblank64x64 frames and preregistered video. Proceed to the frozen100K pilot without parameter changes.
- 本段 compute：`0.166h`
- 动作：`occurred`；完成信号：`pending`

## Checkpoint

- 进展：`metric`；100K pilot completed naturally in1584s and passed integrity at checkpoint step50000. Early0-20K mean83.93 over16 episodes; tail80-100K mean484.85 over32 episodes; improvement400.92. Estimated additional wall3.96h; projected remaining disk about102GiB; all preregistered continuation gates passed. Continue the same replay/checkpoint to500000 decisions without changing algorithm or seed.
- 本段 compute：`0.440h`
- 动作：`occurred`；完成信号：`pending`

## Checkpoint

- 进展：`efficiency`；Full continuation crossed the 250K environment-step monitoring boundary at logged step258048. The original PID remains active; full completion is still pending. Recent policy throughput is about36.5 environment steps/s, replay ratio about520, GPU utilization samples are predominantly90-97%, board memory used26343MiB, and data disk has107GiB available. Intermediate scores are health observations only and do not alter the frozen protocol.
- 本段 compute：`0.667h`
- 动作：`occurred`；完成信号：`pending`

## Checkpoint

- 进展：`efficiency`；Full continuation crossed the 500K environment-step midpoint at logged step503904. The original PID remains active and completion is pending. Policy throughput remains about36.5 environment steps/s, replay ratio about520, recent GPU utilization is mostly86-97%, board memory used26343MiB, and data disk has106GiB available. Intermediate scores and losses remain health observations only; no protocol change or early verdict is authorized.
- 本段 compute：`0.933h`
- 动作：`occurred`；完成信号：`pending`
