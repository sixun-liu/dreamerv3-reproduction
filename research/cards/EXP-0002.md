# EXP-0002：EXP-0001 462K checkpoint fixed-seed eval-only diagnostic

状态：`preregistered`，高成本运行：`not approved`，人工看图：`pending`。

## 一句话问题

EXP-0001保存于462720 environment steps的agent checkpoint能否独立加载，并在固定seed的stochastic-policy eval_only中保持已学会walker_walk的高回报？

## 阶段与基座

- 阶段：`reproduction`
- 当前主要矛盾：区分EXP-0001训练回报成立但checkpoint不可用，与checkpoint策略在独立consumer路径中同样可用两种解释。
- Canonical baseline：`DV3-NATURE-DMCP-WALKER-12M`
- 模板：`probe`；证据权限：`diagnostic_only`
- 假说族：`dmcp-checkpoint-eval`；工作模式：`instrumentation`
- 唯一变量：消费路径从在线训练episode日志切换为只加载agent checkpoint的eval_only；不更新参数。
- 预期动作：从精确checkpoint目录加载agent，使用当前作者代码mode=eval的stochastic action sampling、固定eval seed10000、16 envs和40K agent decisions。
- 完成正向信号：独立logdir自然结束，至少64个完整episode scores、resolved config、stdout与完成信标同时存在。

## 假说

若EXP-0001确实得到可用策略，则checkpoint独立加载后的至少64个完整episodes应全部有限，mean score至少800，且无load、NaN或环境错误。


## 替代解释

- checkpoint位于462720而非终点；固定eval seed与stochastic action sampling可产生低于训练窗口的回报。
- checkpoint参数或当前加载路径不兼容，导致工程失败而非策略无效。

## 控制项

- runtime e3f0224、checkpoint路径、task、模型尺寸、repeat2、eval seed10000和40K decision预算固定。

## 主指标

- complete episode count、score mean/median/std/min/max、有限性、checkpoint load与自然完成状态。

## 证据四联

- 数值/概率：
- 空间/视觉：
- 时序/上尾：
- 因果干预：

## 停止条件

- GPU非空、checkpoint加载失败、NaN/Inf、异常退出或墙钟超过1小时立即停止；不训练、不调参、不替换checkpoint。

## 人工审查

`/root/autodl-tmp/artifacts/dreamerv3/review/EXP-0002-checkpoint-eval`
