# EXP-0017：DreamerV3 Minecraft isolated 100K to 200K increment and evaluation

状态：`preregistered`，高成本运行：用户已通过长期 goal 与 `continue` 授权，人工看图：`pending`。

## 一句话问题

从原始EXP-0012 100K checkpoint与replay以envs=4独立续训至绝对200K后，策略能否在工程完整性不变时维持L1，并在新增训练数据或三回合终点评测中出现wooden-pickaxe/L2证据？

## 阶段与基座

- 阶段：`exploration`
- 当前主要矛盾：EXP-0016已选择envs=4；当前需从原始EXP-0012 100K源独立恢复到绝对200K，并以冻结的三回合评测判断额外100K的学习价值。
- Canonical baseline：`DV3-2026-MINECRAFT-DIAMOND-50M-100K`
- 模板：`formal`；证据权限：`formal_candidate`
- 假说族：`minecraft-incremental-learning`；工作模式：`mixed`
- 唯一变量：训练预算从100K增加到200K environment steps；恢复源、runtime5168475、seed0、size50m、ratio32、reward、wrapper与终点评测协议固定，envs=4仅为已验证执行配置。
- 预期动作：目标logdir完整加载原始100K agent/optimizer/step/replay，以4个环境新增恰好100000个唯一transition并保存step200000 checkpoint；终点checkpoint以agent seed10000单环境评测3个完整回合并固定episode0视频。
- 完成正向信号：EXP-0012源checkpoint/replay哈希前后不变，目标保留100000个旧stepid并恰好新增100000个不重叠stepid，最终checkpoint精确step200000。；训练metrics有限并观察ratio32，资源/OOM/系统盘/临时目录与GPU/Java/Xvfb清场门通过。；独立评测自然完成3回合，evaluation JSON、固定episode0动态视频、100K/200K里程碑和资源对比材料齐全。

## 假说

若额外100K训练继续推动Minecraft技能链，则源100K证据保持不变、目标精确新增100000个唯一transition并结束于200000；L1在独立评测中至少2/3回合出现，且wooden_pickaxe在新增replay或三回合评测至少出现一次。


## 替代解释

- 额外100K只巩固已有L1但仍不足以学会wooden_pickaxe，说明该预算的技能链增量有限而非恢复失败。
- 终点评测世界seed不可控，3回合L1/L2差异可能来自世界与轨迹方差，因此只做描述性里程碑比较，不作逐世界因果归因。

## 控制项

- 固定EXP-0012源树指纹、runtime5168475、MineRL mirror0.4.4、Minecraft Diamond、size50m、seed0、ratio32、动作/reward/break_speed和绝对终点200000。
- 固定agent seed10000、单环境、3回合、每回合36000步和episode0视频；不按中途score或inventory换seed、延长预算或选择最好视频。

## 主指标

- Primary: 工程完整性通过后，L1在200K终点评测中的成功回合数，以及wooden_pickaxe在新增100K replay和三回合评测中的是否/次数。
- Tail: 训练与评测回报分布、cobblestone及更高里程碑、首次出现位置、有限loss/ratio32、墙钟、CPU/RAM/GPU/磁盘与清场。

## 证据四联

- 数值/概率：精确核验源/目标/新增唯一 stepid，统计新增 replay 与三回合终点评测的技能里程碑、回报分布和假说门。
- 空间/视觉：固定展示 episode 0（非 best-of-N）动态视频、首帧与 100K/200K 里程碑对比图。
- 时序/上尾：记录新增 replay 的首次扫描位置、训练/评测尾部、墙钟、CPU 限流、RAM、GPU、磁盘与 OOM。
- 因果干预：唯一学习变量是预算从 100K 增至 200K；恢复源、算法语义与评测协议固定，但世界 seed 不可控，故终点评测差异只作描述性证据。

## 停止条件

- 源指纹漂移、CoW/inode隔离失败、GPU已有任务、根盘低于5GiB、数据盘低于10GiB时不启动。
- OOM、非有限训练值、checkpoint/replay重复或丢失、系统盘泄漏、Minecraft进程残留或训练超过绝对200K时停止并保留证据；不得在同一输出重跑。

## 人工审查

`/root/autodl-tmp/Artifacts/dreamerv3/review/EXP-0017-minecraft-200k-increment`

## 最终裁决

- Verdict：`exact_step_integrity_failed_200008`
- Decision：`invalid_provenance`
- 解释：envs=4恢复训练暴露了同步Driver停止粒度：外层固定请求10步，4环境一次推进12步，从100000到目标200000确定性越界8步；这是执行协议失效，不是策略学习的正负结果。
- 预期动作：`occurred`；完成信号：`failed`
- 进展：`uncertainty_reduction`，用严格门发现并解释了多环境长预算确定性+8步越界，阻止无效checkpoint进入科学评测。
- 耗时：wall `0.480h`；compute `0.478h`
- 观察：训练、保存、源隔离、有限性、ratio32、资源与清场均实际发生并通过。；冻结的唯一硬门要求final step=200000且新增100000 transition；实测均为200008/100008，因此本cycle不得进入评测或学习裁决。
- 限制：本次越界checkpoint和replay完整保留但不作为200K正式checkpoint，不与100K策略做终点评测比较。；吞吐和资源数字可作为工程诊断事实，不能支持L1保持或L2推进主张。
- 下一步：另开低成本instrumentation cycle，在独立runtime修复最后一轮remaining-step停止；以单测和真实envs4恢复smoke证明精确终点后，再从原始EXP-0012新输出重跑正式增量。
- 人工确认：`not_required`
- Scoreboard：`not_required`；Claim registry：`not_required`
