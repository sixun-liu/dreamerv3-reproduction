# EXP-0009：DreamerV3 Reacher Hard 1M three-arm learning-signal pilot

状态：`preregistered`，高成本运行：`approved by user (2026-08-06)`，人工看图：`pending`。

证据权限：仅为单任务、单 seed、论文十分之一预算的 `conceptual pilot`；不得表述为完整复现
Figure 18，也不得推广到其余 13 个任务。

## 一句话问题

At one paired agent/environment seed and 1M environment steps on DMC Reacher Hard, do the three representation-gradient arms show the Figure 18 qualitative ordering?

## 阶段与基座

- 阶段：`exploration`
- 当前主要矛盾：Recover exact Figure 18 learning-signal gradient semantics and test whether one task provides an early qualitative mechanism signal at affordable cost.
- Canonical baseline：`DV3-2024-DMCP-REACHER-12M`
- 模板：`formal`；证据权限：`formal_candidate`
- 假说族：`figure18-learning-signal`；工作模式：`mixed`
- 唯一变量：Representation-gradient routing only: baseline; stop reward plus replay-value gradients; stop decoder reconstruction gradients. All heads continue learning and all other config, agent seed, environment seed, budget, run order, and analysis are fixed.
- 预期动作：First pass algebraic and actual-model parameter-group gradient gates, then run baseline, no_reward_value, and no_reconstruction sequentially for exactly 500000 agent decisions each (1000000 DMC environment steps at repeat 2).
- 完成正向信号：Gradient gate JSON passes all zero/nonzero assertions; all three arm completion beacons exist; each terminal checkpoint is exactly 500000 agent decisions; configs, scores, metrics, stdout, and resource logs are finite and complete.

## 假说

If task-independent reconstruction is the primary representation signal and reward/value gradients are auxiliary on Reacher Hard, baseline and no-reward-value will both exceed no-reconstruction in fixed-bin return AUC, while baseline may further exceed no-reward-value.


## 替代解释

- The 1M pilot may be too short to separate baseline from no-reward-value even if the 10M paper result is valid.
- Single-seed ordering may reflect initialization or DMC stochasticity rather than the intervention.
- The 2024 author reimplementation and replay-critic path may differ from the 2023 Figure 18 code lineage.

## 控制项

- Keep continuation, dynamics/KL losses, actor-critic latent stop policy, optimizer, size12m, task, environment versions, seed 0, 500K-decision budget, and frozen analysis identical across arms.

## 主指标

- Primary: trapezoidal AUC of raw episodic return using fixed 100K environment-step bins over 0-1M. Directional gate: baseline and no_reward_value each exceed no_reconstruction; baseline versus no_reward_value is secondary. Tail: mean raw return for episodes ending in the final 200K environment steps, per-bin episode counts, NaN/Inf, throughput, GPU memory, CPU/RAM, wall time, and disk growth.

## 证据四联

- 数值/概率：固定 100K environment-step 分箱 AUC、末 200K 回报、每箱 episode 数。
- 空间/视觉：三臂同坐标学习曲线，并保留一段 baseline policy rollout 供人工检查。
- 时序/上尾：逐箱曲线、最差箱、NaN/Inf、吞吐、显存、CPU/RAM、wall time 和磁盘增长。
- 因果干预：固定同一 agent/environment seed，只改变 replay representation 的梯度来源。

## 停止条件

- Before compute, abort if any gradient zero/nonzero gate fails, provenance is dirty, GPU is busy, or data disk is below the frozen floor. During a run stop only for OOM, NaN/Inf, traceback/nonzero exit, duplicate GPU process, integrity mismatch, or data disk below 5 GiB; do not stop or tune based on intermediate score.

## 人工审查

`/root/autodl-tmp/artifacts/dreamerv3/review/EXP-0009-reacher-signal-ablation`
