# EXP-0011：DreamerV3 Atari100K Breakout scaled author-reimplementation feasibility

状态：`preregistered`，高成本运行：`not approved`，人工看图：`pending`。

## 一句话问题

在2026作者重实现的size50m/ratio256降规模协议下，Breakout于100K agent decisions内能否形成可复核学习趋势和有效终点行为？

## 阶段与基座

- 阶段：`exploration`
- 当前主要矛盾：DMC Vision已闭环，但尚无Atari低数据预算的环境、模型和行为证据。
- Canonical baseline：`DV3-2026-ATARI100K-BREAKOUT-50M`
- 模板：`replication`；证据权限：`replication_candidate`
- 假说族：`atari100k-breakout-author-reimplementation`；工作模式：`practice`
- 唯一变量：Run the frozen atari100k_breakout task with size50m, ratio256, repeat4 and exactly100K decisions; no within-cycle algorithm or protocol intervention.
- 预期动作：Pass ALE L0; pass an exact2048-decision replay-ready smoke; then complete exactly100000 agent decisions without score-based tuning or restart.
- 完成正向信号：L0 verifies ROM/reset/no-op/image/action/reward/terminal semantics; smoke has exact step2048 checkpoint, nonempty replay and finite post-warmup losses; formal run has exact step100000 checkpoint, raw scores/metrics/resources, official-context analysis, fixed10-episode evaluation and preregistered episode0 video.

## 假说

完整性通过；360K--400K emulator-frame训练回报均值至少3.0，且比0--40K均值至少高1.0；终点独立10局评测与固定视频完整。

## 复现目标

- 类型：`author_reimplementation`
- 目标主张：DreamerV3 learns Atari100K Breakout from pixels under the low-data interaction budget.
- 参考产物：/root/autodl-tmp/Code/DreamerV3/dreamerv3-runtime-2026-crossdomain/scores/atari100k-dreamerv3.json.gz SHA256=c839ef2fd28a5c4b7c6f36151a8d16cb41d4d2fb72338c265850b2b4dd5b5e15
- 协议匹配：task=atari100k_breakout; 64x64 RGB; repeat4; sticky false; random0--30 noops; minimal actions; one env;100K decisions=400K emulator frames. Local size50m/ratio256 differs from paper200M/ratio128 and is a scaled feasibility protocol.


## 替代解释

- Single-seed/no-op RNG variance, modern ALE behavior, smaller model or changed train ratio may explain a positive or negative result relative to the public five-seed curves.

## 控制项

- Freeze runtime5168475, ROM MD5, expanded smoke/formal configs, seed, step units, reference hash, evaluation seeds and no-tuning rule before training outcome inspection.

## 主指标

- Primary: mean raw training score in (360K,400K] emulator frames >=3.0 and at least1.0 above (0,40K]. Tail: integrity, score distribution, checkpoint evaluation, NaN/Inf, throughput, GPU/RAM/disk and fixed episode0 video.

## 证据四联

- 数值/概率：
- 空间/视觉：
- 时序/上尾：
- 因果干预：

## 停止条件

- Stop at the current evidence layer for ROM/license mismatch, invalid observation/action/reward/terminal semantics, OOM, unexpected NaN/Inf, nonzero exit, checkpoint/replay mismatch, duplicate GPU process, projected disk below5GiB or projected wall above12h. Do not tune seed/model/ratio/reward after outcome inspection.

## 人工审查

`/root/autodl-tmp/Artifacts/dreamerv3/review/EXP-0011-atari100k-breakout`

## Checkpoint

- 进展：`efficiency`；ALE L0 completed in the isolated Python3.12/JAX0.6.2 environment. Breakout ROM MD5/SHA256 match; 64x64 RGB frames are nonblank; minimal action set has4 actions; repeat4/sticky-false/no-op0--30/raw-reward configuration is active. A test-only256-frame limit exercised terminal and post-terminal reset; the65-frame video is dynamic. Proceed to the frozen2048-decision replay-ready model smoke.
- 本段 compute：`0.001h`
- 动作：`occurred`；完成信号：`pending`

## Checkpoint

- 进展：`route_closed`；Initial smoke transport exited in0s before environment/model construction because the 2026 runtime rejects the legacy --run.script flag. No GPU process, train directory, replay, score, metric, or scientific training outcome existed. The failed tag is retained. Replacement freeze EVT-0071 changes only the versioned CLI field to top-level --script and uses a new tag/config whose sole semantic difference is logdir.
- 本段 compute：`0.000h`
- 动作：`absent`；完成信号：`pending`

## Checkpoint

- 进展：`route_closed`；Replacement smoke r1 completed model training and produced a complete 2026 directory checkpoint at decision step2050 plus nonempty replay. Integrity recheck confirmed the frozen config and finite episode metrics, but the requested2048 step was rounded to the driver's10-decision boundary and the default120-second logging interval left zero post-warmup loss rows. Retain r1 as an engineering diagnostic only; r2 changes only smoke observability and the attainable diagnostic budget to4090 decisions with log_every15, while formal protocol remains unchanged.
- 本段 compute：`0.038h`
- 动作：`occurred`；完成信号：`failed`

## Checkpoint

- 进展：`efficiency`；Smoke r2 passed all frozen gates at exact decision step4090: directory checkpoint/replay/config integrity passed, six post-warmup loss rows were finite, median steady policy FPS was31.73, projected formal remaining wall was0.84h, projected output15.06GB, and projected remaining disk88.98GB. Peak sampled GPU memory was24635MiB. Proceed to the unchanged seed0 100K-decision formal protocol.
- 本段 compute：`0.057h`
- 动作：`occurred`；完成信号：`pending`
