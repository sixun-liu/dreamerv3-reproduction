# EXP-0015：DreamerV3 Minecraft isolated checkpoint replay recovery

状态：`preregistered`，高成本运行：`not approved`，人工看图：`pending`。

## 一句话问题

能否在不修改EXP-0012源证据的前提下，将其100000步checkpoint和replay隔离恢复到envs=2，并精确新增5040个无重叠transition后自然结束于105040步？

## 阶段与基座

- 阶段：`exploration`
- 当前主要矛盾：EXP-0014已在同预算配对下选择envs=2；当前需从EXP-0012终点隔离恢复checkpoint、replay与step，证明源证据不变且新增transition无重复或丢失，再依据实测ETA冻结有界增量训练。
- Canonical baseline：`DV3-2026-MINECRAFT-DIAMOND-50M-100K`
- 模板：`instrumentation`；证据权限：`diagnostic_only`
- 假说族：`minecraft-recovery-integrity`；工作模式：`instrumentation`
- 唯一变量：从EXP-0012只读源创建独立inode的checkpoint+replay reflink克隆，并将执行环境数从1切换为2；算法、seed0、任务、模型、reward和ratio32保持不变。
- 预期动作：runtime从目标logdir的既有checkpoint和replay加载step、agent与历史transition，两个新worker各自建立新episode/replay链并推进5040步。
- 完成正向信号：源checkpoint/replay树哈希训练前后不变，目标旧100000个stepid全部保留且新增恰好5040个不相交stepid。；最终checkpoint精确step105040，新增子集中至少两个不同chunk前缀带is_first，metrics仅含step>100000。；有限loss与ratio32证据存在，无OOM、系统盘泄漏、临时目录或Java/Xvfb/GPU残留。

## 假说

若同logdir恢复语义和XFS CoW隔离方案正确，则源checkpoint/replay哈希前后不变，目标保留全部100000个旧stepid并恰好新增5040个唯一stepid，其中至少两个新is_first来自不同chunk前缀，最终checkpoint精确为105040。


## 替代解释

- runtime加载agent与step但未正确加载replay，或多环境恢复导致旧transition丢失、重复或链语义混乱。
- CoW克隆、checkpoint清理或replay保存意外修改源run，造成证据污染。

## 控制项

- 固定runtime 5168475、Minecraft Diamond、size50m、seed0、ratio32、wrapper、reward与绝对终点105040。
- 源run只读；禁止from_checkpoint仅加载agent、禁止原地续训、硬链接和105040步以上训练。

## 主指标

- Primary: 源树哈希不变；目标source stepid保留率100%；新增唯一stepid=5040且交集为0。
- Tail: checkpoint step105040、两个新worker is_first前缀、post-source metrics、ratio32、CPU/RAM/GPU/磁盘和清场。

## 证据四联

- 数值/概率：
- 空间/视觉：
- 时序/上尾：
- 因果干预：

## 停止条件

- 源指纹漂移、CoW不可用、inode未隔离、GPU已有任务、根盘<5GiB或数据盘<10GiB时不启动。
- OOM、非有限loss、checkpoint/replay不完整、stepid重复/丢失、系统盘泄漏或Minecraft进程残留时停止并保留证据。

## 人工审查

`/root/autodl-tmp/Artifacts/dreamerv3/review/EXP-0015-minecraft-recovery`

## 最终裁决

- Verdict：`isolated_recovery_integrity_passed`
- Decision：`promising_unresolved`
- 解释：同logdir checkpoint+replay CoW克隆是当前runtime从EXP-0012恢复到envs=2的完整且资源安全方案，可用于另开正式增量训练；本cycle不评价策略质量。
- 预期动作：`occurred`；完成信号：`confirmed`
- 进展：`uncertainty_reduction`，关闭checkpoint/replay/step恢复风险，并用实测稳态吞吐将新增100K成本估为约45至50分钟端到端。
- 耗时：wall `0.080h`；compute `0.079h`
- 观察：EXP-0012源证据保持不变，隔离目标精确从100000恢复并新增5040个唯一transition后结束于105040。；checkpoint、replay forest、post-source metrics、ratio32、资源与清场全部通过冻结门。
- 限制：单次5040步instrumentation诊断，只验证恢复与执行语义；Minecraft world seed未暴露，也没有独立评测或学习效果主张。
- 下一步：从EXP-0012原始100K源另开正式绝对200K cycle，新增100K后做固定agent seed三回合独立评测和展示材料。
- 人工确认：`not_required`
- Scoreboard：`not_required`；Claim registry：`not_required`
